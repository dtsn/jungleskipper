---
label: Attraction
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: members
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: members
  label: Members
